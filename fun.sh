#!/bin/sh

sudo apt install cmatrix					# creates matrix movie effect on screen
sudo apt install screenfetch neofetch		# show ascii system information
sudo apt install fortune 				    # random adage
sudo apt install cowsay 				    # just let cow say something
sudo apt install toilet 				    # draw nice ascii words with styles
sudo apt install figlet 				    # draw nice ascii words with styles
sudo apt install espeak 				    # speech synthesizer
sudo apt install libaa-bin 				    # ascii graphics
sudo apt install sysvbanner 			    # like toilet but limited to 10 chars
sudo apt install aview 					    # images to ascii art
sudo apt install rig 					    # generate fake data real-like


